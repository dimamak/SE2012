entry
	action = entry
	
registration
	action = register
	username,
	password,
	fname,
	sname,
	email

login
	action = login
	username,
	password

logout
	action = logout
	
view list of subforums
	action = viewforum
	
view list of discussions within subforum
	action = viewsubforum
	subforumid

view discussion (message with recoursive comments)
	action = viewdiscussion
	discussionid

add subforum
	action = addsubforum
	title
	
add / edit / remove message 
	action = publish
	subforumid (if new discussion) / 
	parentmsgid(if new comment to old discussion) / 
	messageid(if editing message),
	title, (if title&content empty -> remove message with all comments recoursevly)
	content

search
	action = search
	username(to search it), content
	
add friend???
	action = addfriend
	username

banning
	action = ban
	username, till(if 0 - forever)
	
set moderator
	action = setmoderator
	forumid, username
	
suspend moderator
	action = suspendmoder
	forumid, username, till(if 0 - forever)
	
set admin
	action = setadmin
	username
