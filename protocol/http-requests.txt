entry
	action = entry
	
registration
	action = register
	username,
	password,
	fname,
	sname,
	email

login
	action = login
	username,
	password

logout
	action = logout
	
view list of subforums
	action = viewforum
	
view list of discussions within subforum
	action = viewsubforum
	subforumid

view discussion (message with recoursive comments)
	action = viewdiscussion
	discussionid

add / edit / remove message 
	action = publish
	parentmsgid(if new subforum/message) / 
	messageid(if editing/deleting subforum/message),
	title, (if title&content empty -> remove message with all comments recoursevly)
	content

search
	action = search
	username(to search it), content
	
add friend???
	action = addfriend
	username

banning
	action = ban
	username, till(if 0 - forever)
	
set moderator
	action = setmoderator
	forumid, username
	
suspend moderator
	action = suspendmoder
	forumid, username, till(if 0 - forever)
	
set admin
	action = setadmin
	username
